<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.demo.comentostatistic.dao.StatisticMapper">
    <!--3주차 과제-->
    <select id="selectYearLogin" parameterType="string" resultType="YearCountDto">
        select concat('20', #{year}) as year, count(*) as totCnt
        from statistic.request_info ri
        where left (ri.create_date, 2) = #{year};
    </select>


    <select id="selectYearMonthLogin" parameterType="string" resultType="YearMonthCountDto">
        select concat('20', #{yearMonth}) as yearMonth, count(*) as totCnt
        from statistic.request_info ri
        where left (ri.create_date, 4) = #{yearMonth};
    </select>
    <!-- 4주차 과제-->
    <select id="selectMonthlyLoginCount" resultType="LoginCountDto">
        SELECT DATE_FORMAT(createdate, '%Y-%m') AS `key`,
               COUNT(DISTINCT user_id) AS count
        FROM request_info
        GROUP BY DATE_FORMAT(createdate, '%Y-%m')
        ORDER BY `key`
    </select>

    <select id="selectDailyLoginCount" resultType="LoginCountDto">
        SELECT createdate AS `key`,
               COUNT(DISTINCT user_id) AS count
        FROM request_info
        GROUP BY createdate
        ORDER BY createdate
    </select>

    <select id="selectAvgDailyLoginCount" resultType="double">
        SELECT ROUND(COUNT(*) / COUNT(DISTINCT createdate), 2)
        FROM request_info
    </select>

    <select id="selectDeptMonthlyLoginCount" resultType="LoginCountDto">
        SELECT CONCAT(u.hr_organ, ' : ', DATE_FORMAT(r.createdate, '%Y-%m')) AS `key`,
               COUNT(*) AS count
        FROM request_info r
            JOIN `user` u
        ON r.user_id = u.user_id
        GROUP BY `key`
        ORDER BY `key`
    </select>

    <select id="selectAllLoginDates" resultType="string">
        SELECT createdate
        FROM request_info
    </select>

    <select id="selectHolidays" resultType="string">
        SELECT holiday_date
        FROM holidays
    </select>

</mapper>